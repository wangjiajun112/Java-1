从头开始过代码随想录，用Java
001-二分查找法
给定一个数组查找 指定数，返回这个数在数组中的下标。
class solution{
  public int search(int[] nums ,int target ){
    int left = 0;
    int right = nums.length - 1;

    while(left <= right){ //采用左闭右闭写法
        int mid = (left + right) / 2;
        if(nums[mid] < target){
          left = mid + 1;
    }   else if(nums[mid] > target){
          right = mid - 1;
    }    else{
            return mid;
    }

  }
return -1;
}

Collection集合的体系
Set系列集合的特点： 无序，不重复，无索引

HashSet 集合的底层原理
哈希值就是一个int类型的随机值， Java中每个对象都有一个哈希值。
Java中所有对象，都可以调用Object类提供的hashCode方法， 返回该对象自己的哈希值。
public int hashCode() :

同一个对象多次调用hashCode() 方法返回的哈希值是相同的
不同的对象， 99.9999% 是不相同的

HashSet集合的底层是基于哈希表存储数据的
哈希表是一种增删改查数据性能都比较好的数据结构

HashSet集合元素的去重操作。








